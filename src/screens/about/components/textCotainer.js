import { ContentCotainer } from "common/contentCotainer";
import styled from "styled-components";

export const TextContainer = styled(ContentCotainer)`
  position: absolute; top: 25vh; left: 5vw;
`;
